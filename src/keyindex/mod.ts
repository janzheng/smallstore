/**
 * Key Index Module
 * 
 * Exports all key index functionality (Phase 3)
 */

export {
  saveIndex,
  loadIndex,
  deleteIndex,
  addKeyToIndex,
  removeKeyFromIndex,
  getKeyLocation,
  createEmptyIndex,
} from './storage.ts';

